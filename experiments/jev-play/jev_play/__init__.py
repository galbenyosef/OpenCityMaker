"""Can TypeSafe's `jev` choice model play CityMaker (2048)?

The board goes out as a 2D array with the rules and the objective; one of four
directions comes back. CityMaker's real engine (`src/game/engine.ts`) runs the
game behind an HTTP server, so no browser is involved.
"""
